{
  "name": "virtualboardgames",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "start": "cd games/virtual-game-site && yarn start",
    "build": "lerna run build",
    "postinstall": "yarn run boot",
    "bump": "lerna version",
    "boot": "lerna bootstrap"
  }
}
